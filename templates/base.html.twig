<!DOCTYPE html>
<html lang="fr" data-theme="light">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Gazette - Votre Actualité{% endblock %}</title>
        
        {# SEO Meta Tags #}
        <meta name="description" content="Gazette - Plateforme d'actualité moderne propulsée par Symfony et Prismic.">
        <meta property="og:title" content="Gazette">
        <meta property="og:description" content="Actualité moderne et sécurisée.">
        <meta property="og:type" content="website">

        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%22120%22>📰</text></svg>">
        
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <header>
            <nav class="container" style="display: flex; justify-content: space-between; align-items: center;">
                <a href="{{ path('app_home') }}" style="font-size: 1.5rem; font-weight: 800; text-decoration: none; color: var(--text-main);">GAZETTE</a>
                <div style="display: flex; gap: 2rem;">
                    <a href="{{ path('app_home') }}" style="text-decoration: none; color: var(--text-main); font-weight: 600;">Accueil</a>
                    <a href="#" style="text-decoration: none; color: var(--text-muted); font-weight: 600;">À propos</a>
                    <button onclick="document.documentElement.setAttribute('data-theme', document.documentElement.getAttribute('data-theme') === 'dark' ? 'light' : 'dark')" style="background: none; border: none; cursor: pointer; font-size: 1.2rem;">🌓</button>
                </div>
            </nav>
        </header>

        <main class="container">
            {% block body %}
                <div class="premium-card">
                    <h1>Bienvenue sur Gazette</h1>
                    <p style="font-size: 1.2rem; color: var(--text-muted);">
                        Cette plateforme est en cours de configuration avec <strong>Prismic</strong>. 
                        Veuillez configurer votre <code>PRISMIC_TOKEN</code> et <code>PRISMIC_REPO_NAME</code> dans le fichier <code>.env</code>.
                    </p>
                    <a href="https://prismic.io" class="btn-primary" target="_blank">Accéder à Prismic</a>
                </div>

                {% if content is defined %}
                    <section style="margin-top: 3rem;">
                        <article class="premium-card">
                            {# Here we would render Prismic rich text or data #}
                            <pre>{{ content|json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
                        </article>
                    </section>
                {% endif %}
            {% endblock %}
        </main>

        <footer class="container" style="margin-top: 5rem; border-top: 1px solid var(--border); padding-top: 2rem; color: var(--text-muted); text-align: center;">
            <p>&copy; {{ "now"|date("Y") }} Gazette - Sécurisé par Symfony {{ constant('Symfony\\Component\\HttpKernel\\Kernel::VERSION') }}</p>
        </footer>
    </body>
</html>
